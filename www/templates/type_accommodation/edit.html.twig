{% extends 'base.html.twig' %}

{% block title %}Modification d'une saison
{% endblock %}

{% block body %}
	<div class="accommodation-form">
		<h1 class="form-title text-center">Modifier la saison</h1>
		<div class="form-group">
			{{ include('type_accommodation/_form.html.twig', {'button_label': 'Enregistrer'}) }}
		</div>
		<div class="action-buttons">
			<a href="{{ path('app_type_accommodation_index') }}" class="btn btn-back">
				<i class="fas fa-arrow-left"></i>
				Retour à la liste
			</a>
			{{ include('type_accommodation/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
