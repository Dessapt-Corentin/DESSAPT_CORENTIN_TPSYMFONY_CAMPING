{# On recupère notre base #}
{% extends "base.html.twig" %}

{# on peut redefinir le titre de l'onglet #}
{% block title %}
{{title|default('Nos hébergements')}}
{% endblock %}
{% block body %}
	<div class="container mt-5">
		<header class="mb-4">
			<h1 class="text-center mb-5 display-4">{{title|default('Nos hébergements')}}</h1>
		</header>
		<div class="accommodation-grid">
		{% for accommodation in accommodations %}
			{% include "components/_accommodation_card.html.twig" with {'accommodation': accommodation} %}
		{% endfor %}
		</div>
	</div>
{% endblock %}
